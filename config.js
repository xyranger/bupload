const headless = true;
const cookies = [
    
];
// 间隔执行时间单位分钟
const minute = 2;
// 上传最大百分比；
const process = 10;
// 上传文件名
const fileName = "test.mp4";

module.exports = {
    headless: headless,
    cookies: cookies,
    minute: minute,
    fileName: fileName,
    process: process
}